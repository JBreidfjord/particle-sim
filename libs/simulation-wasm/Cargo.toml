[package]
name = "lib-simulation-wasm"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
lib-simulation-core = { path = "../simulation-core" }
wasm-bindgen = { version = "0.2.79", features = ["serde-serialize"] }
